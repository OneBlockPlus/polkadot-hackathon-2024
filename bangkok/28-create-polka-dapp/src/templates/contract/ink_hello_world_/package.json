{
  "name": "test",
  "version": "1.0.0",
  "description": "This guide walks you through the process of building, deploying, and interacting with an ink! smart contract on a Substrate-based blockchain.",
  "main": "contract.config.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "node utils/deploy.js",
    "interact": "node utils/interact.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@polkadot/api": "^14.0.1",
    "@polkadot/api-contract": "^14.0.1",
    "@polkadot/keyring": "^13.1.1",
    "dotenv": "^16.4.5"
  }
}
