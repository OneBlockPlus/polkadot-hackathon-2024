const OPTIONS = ['Transaction Type', 'Deposit', 'Withdrawal', 'Transfer', 'Payment']

const STEP = 1
const MIN = 1
const MAX = 100

const APP_KEY = 'uNFT-user'

export { STEP, MIN, MAX, OPTIONS, APP_KEY }
