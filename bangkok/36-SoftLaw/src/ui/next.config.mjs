/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['harlequin-quiet-smelt-978.mypinata.cloud'], // Add your image domain here
    },};

export default nextConfig;
